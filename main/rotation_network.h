#ifndef ROTATION_NETWORK_H
#define ROTATION_NETWORK_H

// Only for timer/ROTATE wakes, with the periodic timer stopped and no
// pre-existing HA server/notification obligation.
void rotation_network_done(void);

#endif
